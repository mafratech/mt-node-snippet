language: node_js

node_js:
  - "12"

branches:
  only:
    - master

cache:
  npm: true

script:
  - echo "skipping tests"

before_publish:
  - npm intall -g add vsce

publish:
  - echo $TOKEN
  - vsce package
  - vsce publish -p $TOKEN
